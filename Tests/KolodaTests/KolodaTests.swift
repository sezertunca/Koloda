    import XCTest
    @testable import Koloda

    final class KolodaTests: XCTestCase {
        func testExample() {
            // This is an example of a functional test case.
            // Use XCTAssert and related functions to verify your tests produce the correct
            // results.
            XCTAssertEqual(Koloda().text, "Hello, World!")
        }
    }
